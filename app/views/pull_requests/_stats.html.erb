<div id="stats">
  <span><strong><%= pluralize @pull_request.file_count, "file" %></strong></span>
  <span class="addition">with <strong><%= pluralize @pull_request.total_additions, 'addition'  %></strong></span>
  <span clas="deletion">and <strong><%= pluralize @pull_request.total_deletions, 'deletion' %></strong></span>
</div>